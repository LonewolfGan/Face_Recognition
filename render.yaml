services:
  - type: web
    name: face-recognition-api
    runtime: python
    plan: free
    buildCommand: pip install -r backend/requirements.txt
    startCommand: gunicorn --chdir backend run:app --bind 0.0.0.0:$PORT --workers 1 --timeout 300
    envVars:
      - key: FLASK_ENV
        value: production
      - key: SECRET_KEY
        generateValue: true
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: DATABASE_URL
        sync: false
      - key: DATA_DIR
        value: /opt/render/project/src/backend/data/
      - key: CORS_ORIGINS
        sync: false
      - key: MODEL_NAME
        value: ArcFace
      - key: DETECTOR_BACKEND
        value: opencv
      - key: PYTHON_VERSION
        value: "3.11"
    healthCheckPath: /api/health
    autoDeploy: false
